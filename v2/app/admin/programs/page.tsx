'use client';
import {useEffect,useState} from 'react';
export default function Programs(){const[rows,setRows]=useState<any[]>([]);const[form,setForm]=useState({title:'',gujaratiTitle:'',date:new Date().toISOString().slice(0,10),time:'',location:'',type:'',description:'',status:'PUBLISHED',visibility:'OWNER'});const[error,setError]=useState('');async function load(){const r=await fetch('/api/admin/events');if(r.ok)setRows(await r.json());else setError('Organizer access required')}useEffect(()=>{load()},[]);async function save(e:any){e.preventDefault();setError('');const r=await fetch('/api/admin/events',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,date:new Date(form.date).toISOString()})});if(!r.ok){setError('Could not save program');return}setForm({...form,title:'',gujaratiTitle:'',time:'',location:'',type:'',description:''});load()}return <div className="main"><div className="page-title"><div><p className="eyebrow">SOCIETY • PROGRAMS</p><h1>Programs / કાર્યક્રમો</h1><p>Upcoming society functions અને events manage કરો.</p></div></div><div className="card program-form" style={{marginBottom:18}}><div className="section-head"><div><p className="eyebrow">CREATE • SCHEDULE</p><h2>Add Society Program</h2><p className="section-subtitle">Create an event residents can discover from the dashboard.</p></div></div><form onSubmit={save} className="form-grid"><div className="field"><label>Program Title</label><input className="input" required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></div><div className="field"><label>Gujarati Title</label><input className="input" value={form.gujaratiTitle} onChange={e=>setForm({...form,gujaratiTitle:e.target.value})}/></div><div className="field"><label>Date</label><input className="input" type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></div><div className="field"><label>Time</label><input className="input" value={form.time} onChange={e=>setForm({...form,time:e.target.value})}/></div><div className="field"><label>Location</label><input className="input" value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/></div><div className="field"><label>Type</label><input className="input" value={form.type} onChange={e=>setForm({...form,type:e.target.value})}/></div><div className="field"><label>Status</label><select className="input" value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option>PUBLISHED</option><option>DRAFT</option></select></div><div className="field" style={{gridColumn:'1/-1'}}><label>Description</label><textarea className="input" rows={3} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div><button className="btn btn-primary">＋ Add Program</button></form>{error&&<p className="login-error">{error}</p>}</div><div className="program-grid">{rows.map(x=><article className="card program-card" key={x.id}><div className="program-top"><span className="badge">{x.status}</span><span className="program-type">{x.type||'Society Event'}</span></div><h3>{x.gujaratiTitle||x.title}</h3>{x.gujaratiTitle&&<p className="program-en">{x.title}</p>}<div className="program-meta"><span>📅 {new Date(x.date).toLocaleDateString('en-IN')}</span><span>🕒 {x.time||'Time TBA'}</span><span>📍 {x.location||'Location TBA'}</span></div>{x.description&&<p className="program-desc">{x.description}</p>}</article>)}{!rows.length&&<div className="card">No programs added yet.</div>}</div></div>}

const programStyle=`.program-form{border-top:3px solid #b18a3a}.program-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px}.program-card{min-height:190px}.program-top{display:flex;justify-content:space-between;gap:8px;align-items:center}.program-type{font-size:12px;font-weight:700;letter-spacing:.05em;text-transform:uppercase;opacity:.58}.program-card h3{margin:16px 0 4px;font-size:20px}.program-en{margin:0 0 14px;opacity:.62}.program-meta{display:grid;gap:7px;font-size:13px;color:#746b65}.program-desc{margin-top:14px;padding-top:12px;border-top:1px solid #e6ddd3;font-size:13px}.grid{display:grid;gap:16px}@media(max-width:900px){.program-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:600px){.program-grid{grid-template-columns:1fr}.program-form .form-grid{grid-template-columns:1fr}.program-form .form-grid [style*="grid-column"]{grid-column:auto!important}}`;if(typeof document!=="undefined"&&!document.getElementById("program-page-style")){const s=document.createElement("style");s.id="program-page-style";s.textContent=programStyle;document.head.appendChild(s)}
