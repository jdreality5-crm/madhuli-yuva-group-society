'use client';
import {useEffect,useState} from 'react';
export default function Flats(){const[rows,setRows]=useState<any[]>([]);const[form,setForm]=useState({flatNumber:'',ownerName:'',mobile:'',email:'',status:'ACTIVE'});const[error,setError]=useState('');async function load(){const r=await fetch('/api/admin/flats');if(r.ok)setRows(await r.json());else setError('Organizer access required')}useEffect(()=>{load()},[]);async function save(e:any){e.preventDefault();setError('');const r=await fetch('/api/admin/flats',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(!r.ok){setError('Could not save flat. Check flat number and details.');return}setForm({flatNumber:'',ownerName:'',mobile:'',email:'',status:'ACTIVE'});load()}return <div className="main"><div className="page-title"><div><p className="eyebrow">SOCIETY • MEMBERS</p><h1>Flats & Owners / ફ્લેટ અને સભ્યો</h1><p>Flat, owner અને contact details manage કરો.</p></div></div><div className="card" style={{marginBottom:18}}><form onSubmit={save} className="form-grid"><div className="field"><label>Flat Number</label><input className="input" required value={form.flatNumber} onChange={e=>setForm({...form,flatNumber:e.target.value})}/></div><div className="field"><label>Owner Name</label><input className="input" value={form.ownerName} onChange={e=>setForm({...form,ownerName:e.target.value})}/></div><div className="field"><label>Mobile</label><input className="input" value={form.mobile} onChange={e=>setForm({...form,mobile:e.target.value})}/></div><div className="field"><label>Email</label><input className="input" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="field"><label>Status</label><select className="input" value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option>ACTIVE</option><option>INACTIVE</option></select></div><button className="btn btn-primary">Add Flat / Owner</button></form>{error&&<p className="login-error">{error}</p>}</div><div className="table-wrap"><table className="table"><thead><tr><th>Flat</th><th>Owner</th><th>Mobile</th><th>Email</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><b>{x.flatNumber}</b></td><td>{x.ownerName||'—'}</td><td>{x.mobile||'—'}</td><td>{x.email||'—'}</td><td>{x.status}</td></tr>)}{!rows.length&&<tr><td colSpan={5}>No flats added yet.</td></tr>}</tbody></table></div></div>}
