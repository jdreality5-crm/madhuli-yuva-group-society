# Flat Owner Signup

Public signup creates only `OWNER` accounts.

A flat must already exist in the society database and be active. The submitted email/mobile must match the contact details pre-registered by an administrator. Master Admin and Sub Admin accounts cannot be created through this flow.
