'use client';
import {useEffect,useState} from 'react';
import {UiIcon} from '@/components/UiIcon';
type Item={id:string;name:string;propertyNumber:string;block?:string|null;type:string;unit:{id:string;label:string;floorNumber?:number|null;floorLabel?:string|null;residentType?:string|null}};
export default function PropertiesPage(){const[data,setData]=useState<Item[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{fetch('/api/properties',{cache:'no-store'}).then(async r=>{const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Unable to load residence');setData(d.properties||[])}).catch(e=>setError(e.message||'Unable to load residence')).finally(()=>setLoading(false))},[]);return <div className="app-shell"><header className="topbar"><div className="brand"><div className="brand-mark">S</div><div className="brand-copy">My Society<small>માધુલી યુવા ગ્રુપ</small></div></div><a className="btn btn-secondary" href="/">Dashboard</a></header><main className="main" style={{maxWidth:1100,margin:'0 auto'}}><div className="page-title"><div><p className="eyebrow">MY ACCOUNT</p><h1>My Property</h1><p>Your registered residence and property details.</p></div></div>{loading?<div className="card" style={{padding:32}}>Loading residence…</div>:error?<div className="card" style={{padding:32,color:'var(--maroon)'}}>{error}</div>:!data.length?<div className="card" style={{padding:32}}>No active residence is linked to your account.</div>:<div style={{display:'grid',gap:18}}>{data.map(x=><section className="card" key={x.id} style={{padding:24}}><div style={{display:'flex',alignItems:'center',gap:14,borderBottom:'1px solid var(--line)',paddingBottom:18,marginBottom:18}}><div style={{width:48,height:48,borderRadius:14,display:'grid',placeItems:'center',background:'var(--maroon-soft)',color:'var(--maroon)'}}><UiIcon name="home" size={22}/></div><div><p className="eyebrow" style={{margin:0}}>{x.type==='TENAMENT'?'TENAMENT':'APARTMENT'}</p><h2 style={{margin:'3px 0 0'}}>{x.name}</h2></div></div><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(190px,1fr))',gap:14}}><div><small>Property</small><strong style={{display:'block',marginTop:4}}>{x.propertyNumber}</strong></div>{x.block&&<div><small>Block</small><strong style={{display:'block',marginTop:4}}>Block {x.block}</strong></div>}<div><small>Unit</small><strong style={{display:'block',marginTop:4}}>{x.unit.label}</strong></div>{(x.unit.floorLabel||x.unit.floorNumber!==null&&x.unit.floorNumber!==undefined)&&<div><small>Floor</small><strong style={{display:'block',marginTop:4}}>{x.unit.floorLabel||('Floor '+x.unit.floorNumber)}</strong></div>}<div><small>Resident type</small><strong style={{display:'block',marginTop:4}}>{x.unit.residentType==='TENANT'?'Tenant':'Owner'}</strong></div></div></section>)}</div>}</main></div>}